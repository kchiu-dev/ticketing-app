extend type Ticket @key(fields: "ticketId") {
  ticketId
}

type Order @key(fields: "orderId") {
  orderId
  status
  ticket
}

ticketId: string @index(exact) @external .
orderId: string @index(exact) .
status: string @index(term) @upsert .
ticket: uid .