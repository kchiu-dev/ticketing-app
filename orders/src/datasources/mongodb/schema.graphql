type Ticket @entity {
  ticketId: ID! @id
  title: String! @column
  price: Float! @column
}

enum OrderStatus {
  CREATED
  CANCELLED
  COMPLETE
}

type Order @entity {
  orderId: ID! @id
  status: OrderStatus! @column
  ticket: Ticket! @link
}
